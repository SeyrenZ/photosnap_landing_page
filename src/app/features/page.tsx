import { FeaturesHeader } from "@/components";

import React from "react";

const page = () => {
  return (
    <div className="pt-[72px]">
      <FeaturesHeader />
    </div>
  );
};

export default page;
